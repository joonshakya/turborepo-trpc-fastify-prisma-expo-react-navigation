{
  "name": "@acme/api",
  "version": "0.1.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "build": "esbuild index.ts --bundle --packages=external --platform=node --format=esm --outdir=dist --sourcemap",
    "dev": "tsx watch --clear-screen=false index",
    "start": "node dist/index.js",
    "clean": "rm -rf .turbo node_modules",
    "lint": "eslint .",
    "format": "prettier --check \"**/*.{mjs,ts,md,json}\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/websocket": "^7.1.2",
    "fastify": "^4.13.0",
    "@acme/db": "^0.1.0",
    "@trpc/server": "^10.40.0",
    "jsonwebtoken": "^9.0.2",
    "superjson": "1.13.1",
    "zod": "^3.22.2",
    "tslib": "^2.5.0"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.4",
    "esbuild": "^0.17.10",
    "@acme/eslint-config": "^0.2.0",
    "@acme/prettier-config": "^0.1.0",
    "@acme/tsconfig": "^0.1.0",
    "tsx": "^3.12.7",
    "eslint": "^8.51.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "eslintConfig": {
    "root": true,
    "extends": [
      "@acme/eslint-config/base"
    ]
  },
  "prettier": "@acme/prettier-config"
}
